.env
.deploy.lock
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.DS_Store

# Working folder for brand source files — exports live in static/brand/.
assets/

# Uploaded kitchen photos and their renders. Never in git, never in static/.
data/

# Factory galleries: drawn on the server by scripts/make_gallery.py from the
# spec in scripts/galleries/, never committed. The spec is the code.
static/galleries/blinds/
